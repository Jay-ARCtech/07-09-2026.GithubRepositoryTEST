node_modules/
dist/
.DS_Store
*.log

# Capacitor native projects are generated by `npx cap add <platform>`.
# They are reproducible from capacitor.config.ts + dist/ and contain
# machine-specific paths, so they are not committed here.
ios/
android/
.vite/
coverage/
